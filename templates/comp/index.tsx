import templatename from "./templatename";
import templatenameReducer from "./templatenameReducer";
import * as types from "./types";

const templatenameComp = {
  components: {
    templatename,
    templatenameReducer,
  },
  types,
};

export default templatenameComp;
